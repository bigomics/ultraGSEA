# Superfast version of Fisher Exact Test from 'corpora' R package.

Superfast version of Fisher Exact Test from 'corpora' R package.

## Usage

``` r
corpora.fisher.pval(
  k1,
  n1,
  k2,
  n2,
  alternative = c("two.sided", "less", "greater"),
  log.p = FALSE
)
```
